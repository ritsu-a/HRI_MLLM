Install:
    tested on python=3.10, CUDA=12.1, pytorch=2.6

    pip install torch=2.6 torchvision=0.21 torchaudio=2.6
    pip install -re requirements.txt
    pip install -e .

    cd HRI_mllm/external/HRI_retarget
    git checkout g1_hri_mllm(maybe not need this line?)
    pip install -e .


Inference:
    python HRI_mllm/scripts/test.py


TODO:
    inference is too slow 

    monkey-patch qwen2.5omni (or even change the raw class?)

    update and test motion encoder(mostly vae)
